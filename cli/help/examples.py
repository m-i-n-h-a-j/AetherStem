EXAMPLES = {
    "separate_cpu": "aetherstem separate song.flac --backend onnx --device cpu --chunk-size 441000",
    "separate_low_memory": "aetherstem separate song.wav --low-memory --overlap 0.25",
    "runtime_diagnostics": "aetherstem runtime-diagnostics",
    "agent_metadata": "aetherstem ai-metadata",
}

